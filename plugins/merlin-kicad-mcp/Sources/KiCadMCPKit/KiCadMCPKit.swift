/// Marker namespace for the KiCadMCPKit library — the MCP server for Merlin's KiCad
/// electronics domain. Protocol core lives in `MCPServer`, the tool surface in
/// `KiCadTools`.
public enum KiCadMCPKit {
    public static let protocolName = "merlin-kicad-mcp"
}
